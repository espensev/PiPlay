# CI and feature workflow - plan

**Spec:** `docs/superpowers/specs/2026-06-06-ci-and-feature-workflow-design.md`

- [x] Add a GitHub Actions workflow that runs Lane A and the non-mutating build gate on Windows.
- [x] Add a feature workflow document for contributors and agents.
- [x] Link the new workflow document from the root and docs indexes.
- [x] Update existing working/testing docs so they no longer say CI does not exist.
- [x] Verify with `dotnet test`, `Build-PiPlay.ps1 -Stage Build`, and whitespace checks.
