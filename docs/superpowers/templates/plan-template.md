# <Feature> — implementation plan

Copy to `docs/superpowers/plans/YYYY-MM-DD-<topic>.md` only for multi-step work. Delete the plan after implementation; move any durable decision, constraint, command, or open issue to a canonical document.

**Spec:** `docs/superpowers/specs/YYYY-MM-DD-<topic>-design.md`

**Goal:** <One-sentence implementation outcome.>

## Tasks

Order tasks so each leaves the tree green and is independently committable.

- [ ] **Task 1 — <verb-led title>**
  - Files/interfaces: <exact scope>
  - Change: <required behavior>
  - Verify: `<exact command>` and <manual gate, if any>
  - Commit: `<type(scope): summary referencing the requirement ID>`

- [ ] **Task 2 — <verb-led title>**
  - Files/interfaces: <exact scope>
  - Change: <required behavior>
  - Verify: `<exact command>` and <manual gate, if any>
  - Commit: `<type(scope): summary referencing the requirement ID>`

## Unresolved blockers

- None.
