# Evidence

This folder is for current, build-specific QA evidence that needs to ship with a release or review
branch. Keep it sparse: fold old evidence into the relevant active docs or change-pass record, then
delete stale screenshots and one-off notes.

Use build-specific names, for example `v0.6.0-b22-main-chrome.png`, not generic `current` filenames.
