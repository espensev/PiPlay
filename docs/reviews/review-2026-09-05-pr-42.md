# Review - PiPlay PR #42

**Date:** 2026-09-05
**PR:** [#42 - docs: consolidate guidance and define promotion flow](https://github.com/espensev/PiPlay/pull/42)
**Surface:** fixed PR diff, merge base `42dea7be1ff812d91b5a86f33d94b01a9c95266a` to head `ed296d1861e0f68a443d948716ad3fabab358934`.
**Spec source:** PR #42 body; `docs/PiPlay_Product_Engineering_Spec.md`; retired privacy, acceptance, and unresolved-work documents at the merge base.
**Standards sources:** `CLAUDE.md`, `docs/AGENTS.md`, `docs/DECISIONS.md`.
**Verdict:** FAIL - one medium-priority packaging regression. The PR is already merged; correct it in a follow-up.

## Findings

### Medium

1. **[P2] [axis: regression/spec] Package the replacement privacy guidance.**
   Location: `scripts/Build-PiPlay.ps1:67-71`.

   Removing `docs/Data_and_Privacy_Map.md` from `PublishExtras` drops the previously shipped description of data locations, retained browser sessions, diagnostics, and the difference between resetting app state and clearing browser data. The PR moves this material into `docs/PiPlay_Product_Engineering_Spec.md:177-191`, with data-root resolution in `docs/DECISIONS.md:31`, but neither replacement is packaged. `Copy-PublishExtras` at `scripts/Build-PiPlay.ps1:377-399` copies only the enumerated files; the project file contains no alternative Markdown publish rule.

   Evidence: executed the unchanged `Copy-PublishExtras` function with the actual PR-head `PublishExtras` array into a fresh local evidence directory, without executing the build/deployment entry point. It produced:

   ```text
   README.md
   docs\CHANGELOG.md
   docs\YouTube_Compliance.md
   privacy_map_present=False
   replacement_spec_present=False
   privacy_guidance_matches=0
   ```

   The last count searches all copied documents for `WebView2UserData`, `Reset app state`, `Clear browser data`, `telemetry`, and `settings.json`. The README's relative product-spec link also has no target in this package; that missing link predates this PR, but becomes the only remaining route to the relocated privacy documentation after this deletion.

   Impact: a recipient of the release archive loses the bundled guidance needed to understand which local files contain browser sessions and what each privacy action removes. This contradicts the consolidation intent at the delivered-documentation surface, even though the repository retains the facts.

   Recommendation: include a self-contained privacy guide in the package, or package the consolidated specification and its required data-root references. Verify the copied release documentation, not only links in the repository.

### High / Low

No additional findings. No application-code behavior change was found in this PR.

## Verification

| Check | Result |
|---|---|
| `gh pr list --repo espensev/PiPlay --state all --limit 10 --json ...` and `gh pr view 42 --repo espensev/PiPlay --json ...` | No open PRs; #42 is the latest PR and was merged at `2026-09-01T23:14:07Z`. The user did not specify a number; #42 was the stated review assumption. |
| `git merge-base a9e50add9ffdaa819b13da3a78c37e813a88ba04 HEAD` | `42dea7be1ff812d91b5a86f33d94b01a9c95266a`; 20 changed files, 76 insertions, 441 deletions. |
| `git diff --check 42dea7b...HEAD` | Pass. |
| `git diff --quiet HEAD origin/main` | Exit 0: the PR-head tree matches merged main `3fa16131f27e5ff3ded787babab43694f6b3d4f0`. |
| Relative Markdown file-link check over tracked Markdown | 8 files, 10 relative links, 0 missing repository targets. External URLs and fragment validation excluded. |
| Compare `REQ-APP-01` and `REQ-APP-02` lines between merge base and head | Both unchanged. |
| Compare changed C# files after removing full-line comments | All 3 have identical noncomment text. |
| PowerShell parser checks on both changed scripts | 0 errors in `Build-PiPlay.ps1` and `Test-UiSmoke.ps1`. |
| Isolated execution of the AST-extracted `Copy-PublishExtras` function and actual extras array | Reproduced the missing replacement privacy documentation. Evidence retained under `.git/review-evidence/pr42-package/`. No build, process-stop, release, or deployment entry point invoked. |
| `pwsh -NoProfile -File ./scripts/Test-LocalCI.ps1 -Plan` | Pass; command plan only, not a local test/build pass. |
| `gh run view 33443617927 --repo espensev/PiPlay --log` | Historical PR CI log independently retrieved: 1,146 passed, 0 failed, 0 skipped; Release build 0 warnings and 0 errors; `LOCAL CI: PASS`. [Run](https://github.com/espensev/PiPlay/actions/runs/33443617927). |
| Full local CI / deployed UI or audio acceptance | Not run: the behavioral source diff is comments only, and the relevant packaging behavior was exercised directly. Existing CI is reported as historical evidence, not a fresh local or deployed result. |

The initially empty local PiPlay directory was cloned for this review and checked out detached at the PR head. Discovery probes before cloning correctly reported no Git repository. `scripts/Test-Documentation.ps1` and `.github/pull_request_template.md` are absent at this head; neither was treated as an available verification gate. A wildcard path passed to `rg` was rejected; subsequent checks used explicit paths and tracked files.

## Coverage

All 20 changed files are accounted for below.

Reviewed changed content and relevant surrounding contracts deeply:

- `CLAUDE.md`
- `README.md`
- `docs/AGENTS.md`
- `docs/CHANGELOG.md`
- `docs/DECISIONS.md`
- `docs/Data_and_Privacy_Map.md` (deleted; checked migration and packaging)
- `docs/PiPlay_Product_Engineering_Spec.md`
- `docs/QA_Checklist.md` (deleted; checked retained acceptance instructions)
- `docs/SPEC_GAPS_AND_OWNERSHIP.md` (deleted; checked carried unresolved/deferred items)
- `docs/Theme_Preset_Differences.md`
- `docs/YouTube_Compliance.md`
- `scripts/Build-PiPlay.ps1`
- `scripts/Test-UiSmoke.ps1`
- `src/PiPlay/Services/AppPaths.cs`
- `src/PiPlay/Services/PrivacyService.cs`
- `src/PiPlay/Services/WindowOpacityPolicy.cs`

Reviewed the following deleted records for retirement/carryover impact; historical runtime and prior-review claims were sampled, not independently replayed:

- `docs/reviews/qa-2026-08-26-stable-v0.13.2-b39-listening.md`
- `docs/reviews/review-2026-08-22-merged-prs-33-34-35.md`
- `docs/reviews/review-2026-08-26-plans-and-cleanup.md`
- `docs/reviews/review-2026-08-26-pr-36-takeover.md`

Supporting inspection included the project publish items, CI workflow, release-script policy tests, settings atomic write, theme resource publication, and privacy action copy. The Q-1 limitations and provisional profile-selector acceptance remain in spec section 24; their current deployed state was not checked.

## Surfaces and next step

Only the local review checkout, ignored packaging evidence, and this report were created. No application source was edited; no runtime, remote branch, PR, release, or deployment was changed. Address the packaged privacy-documentation regression in a follow-up change.
