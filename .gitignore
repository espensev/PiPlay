## .NET / Visual Studio
bin/
obj/
.vs/
*.user
*.suo

## Test run output (vstest --blame dumps, TRX, coverage)
[Tt]est[Rr]esults/

## Build output
[Dd]ebug/
[Rr]elease/
x64/
x86/

## Rider
.idea/

## Local AI-agent settings (Claude Code). Personal, machine-local; never shared.
.claude/settings.local.json

## Local app data (runtime)
*.log

## Stray local archives (release zips live under bin\publish\archive, which is ignored)
docs.zip
piplay-theme-v2-tight-scope-docs.zip
Archive/

## Transient agent check scripts (throwaway re-derivation / verification helpers, never shared)
.tmp_*

## Transient UI-smoke captures (Test-UiSmoke.ps1 writes a timestamped shot per run).
## Curated, stable-named evidence is committed deliberately; these raw runs are not.
docs/evidence/ui-smoke-*.png

## Owner's raw review inbox. Never shared; only unresolved factual reviews are promoted temporarily.
PRI-READ/

## Owner's loose visual references dropped at the repo root (kept on disk, never shared).
## A clean tree is a release-publish gate, so these must not read as untracked drift.
46420_edited.jpg
Screenshot 2026-07-03 020846_edited.png
themes.......png
